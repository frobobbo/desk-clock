apiVersion: v1
kind: Service
metadata:
  name: {{ include "desk-clock-config.fullname" . }}
  labels:
    {{- include "desk-clock-config.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "desk-clock-config.selectorLabels" . | nindent 4 }}

